const first_number: number = 20
const second_number: number = 30
const sum = (first: number, second: number): string => {
  return `${first} + ${second} = ${first + second}`
}
console.log(sum(first_number, second_number))
